import React from 'react';
import ProjectMasterCard from './ProjectMasterCard'; // assuming the component file is named ProjectMasterCard.js

const SampleProjects = () => {
  

  return <ProjectMasterCard projects={projects} />;
};

export default SampleProjects;
